# P6.2 Production-like Qualification

**Status:** COMPLETE
**Started:** 2026-07-27
**Completed:** 2026-07-28
**Decision:** Filesystem fault-injection, recovery, the installed-pilot
harness, and the ADR-0012 accelerated qualification pass. P6.2 is complete for
the bounded declarative-local slice. Seven-day resilience and production
readiness remain unclaimed.

## Objective

P6.2 qualifies the completed declarative-local extension slice under bounded
operational conditions:

- interrupted writes must fail closed without losing the recovery path;
- recovery must retain manifest, authority, Work, Run, and Audit provenance;
- a reconciled extension Work must be safely replayable with a new immutable
  Run and Extension Result revision;
- repeated fixed-snapshot pilots must expose latency and control-data growth;
  and
- accelerated evidence and calendar-duration evidence must remain separate so
  that neither can be mistaken for the other.

This scope remains local-only. It does not add network access, arbitrary
commands, provider mutation, distributed execution, or production authority.

## Pinned official evidence lane

The official duration subject is isolated from later source changes under:

```text
D:\AOS-PILOT-EVIDENCE\p6-2-production-like-official\
`-- pinned\4031c3e\
```

The pin records source commit
`4031c3e596dc5dc846d46a0c115dac50088bf19c`, the binary, both reference
manifests, both pilot harnesses, and their SHA-256 values in
`pin-manifest.json`. The pinned binary SHA-256 is
`df95e23710c9eaf063cb78a3431b14ccb6d72bbf84cc08a5a9c6537bd239353c`.

The historical daily lane used three clean detached repositories under
`p6-2-production-like-official\repositories`, verified their exact pinned
commits before every run, and failed closed on commit or dirty-state drift.
Its automation was retired after ADR-0012 closed P6.2 through the independent
accelerated lane. The two daily samples and their real timestamps remain
immutable evidence; they were not copied into or rewritten as accelerated
samples.

ADR-0011 permits isolated P6.3 installation work in parallel. It does not
change the official subject, synthesize elapsed days, or weaken this gate.
ADR-0012 supersedes only the seven-day blocking exit condition and preserves
this evidence boundary.

Official state after sample `20260728T034915926Z`:

```text
repositories: 3 fixed detached snapshots
passed samples: 2
failed samples: 0
result integrity failures: 0
total governed Extension Runs: 24
source repository mutations: 0
observed duration: 0.762998 days
latency per Extension Run p50: 336.683 ms
latency per Extension Run p95: 921.258 ms
latest sample control data: 119,393 bytes
duration gate: ACTIVE
```

The latest nested result SHA-256 is
`03dee41d845a137acfde3c5bd50a683478aa0f12be529040d483a226632ada92`.
The stored result digest was re-read successfully after the sample. All three
detached repositories remained clean at their pinned commits.

## Accelerated qualification closeout

ADR-0012 authorizes a separate accelerated lane because calendar waiting does
not execute additional fault classes for this deterministic local-only slice.
The lane uses the same pinned binary, manifests, harnesses, and three detached
repository commits as the historical daily lane.

Canonical command:

```powershell
.\scripts\run_p6_2_accelerated_qualification.ps1
```

Result:

```text
pilot: p6-2-accelerated-qualification
samples: 8/8 PASS
failed samples: 0
integrity failures: 0
governed Extension Runs: 96
source mutations: 0
deterministic nested capabilities: 100%
result digests revalidated: 8/8
latency per Extension Run p50: 374.319 ms
latency per Extension Run p95: 423.192 ms
minimum sample latency: 365.717 ms
maximum sample latency: 423.192 ms
total downstream control data: 955,164 bytes
observed duration: 0.000379 days
qualification gate: PASS
```

State:

```text
D:\AOS-PILOT-EVIDENCE\p6-2-accelerated-qualification\pilot-state.json
```

Marker:

```text
AOS_P6_2_ACCELERATED_QUALIFICATION_OK
```

The `0.000379` observed days are reported specifically to prevent this evidence
from being described as a seven-day soak. The accepted claim is accelerated
production-like functional qualification for the bounded local declarative
extension slice.

## Recovery hardening

An idempotent extension enable now requires all four persisted facts:

```text
enabled lifecycle
+ matching immutable manifest digest
+ matching Governance enable decision
+ matching Audit enable event
```

If lifecycle reached `enabled` but Governance or Audit was interrupted, the
next governed enable revalidates the manifest, appends recovery lifecycle
revisions, and records digest-bound Governance and Audit evidence. It emits:

```text
extension:incomplete-trace-recovered
```

Reconciliation decisions for extension Work now retain the immutable extension
reference, manifest digest, capability, resource scope, and previous authority
basis. Run revision discovery uses the persisted Run identity, so a resolved
Work writes a new Run and Extension Result revision instead of colliding with
the interrupted attempt.

## Filesystem fault-injection gate

The process tests inject an ordinary file where AOS expects each tested
directory:

| Boundary | Expected behavior | Local result |
| --- | --- | --- |
| Manifest snapshot | Exit 8, no unsafe continuation, retry succeeds | PASS |
| Lifecycle revision | Exit 8, retained manifest is reusable | PASS |
| Governance trace | Partial enable is detected and repaired | PASS |
| Audit trace | Partial enable is detected and repaired | PASS |
| Extension Result | Work becomes blocked with reconciliation required | PASS |

After restoring the Result boundary, an attributable reconciliation returns the
Work to `authorized`; replay completes and persists result revision `r2`.

Canonical command:

```powershell
.\scripts\run_p6_2_fault_injection_gate.ps1
```

Markers:

```text
AOS_P6_2_FILESYSTEM_FAULT_INJECTION_OK
AOS_P6_2_RECONCILIATION_OK
```

This is deterministic filesystem boundary injection. Forced process
termination at every instruction boundary and real disk-full hardware
qualification remain outside the current proof.

## Installed-pilot qualification harness

Canonical sample command:

```powershell
.\scripts\run_p6_2_installed_pilot_sample.ps1 `
  -RepositoryPath @('D:\AOS','D:\trenux_rust','D:\Project\TRENUX') `
  -SamplesPerRun 3 -Repeats 2 `
  -MinimumDurationDays 7 -MinimumSamples 7 `
  -PilotId p6-2-production-like
```

Immutable per-sample observations and their derived pilot state are kept
outside the source repository by default:

```text
%TEMP%\aos-p6-2-production-like-qualification\
  p6-2-production-like\
    samples\<sample-id>.json
    raw\<sample-id>\...
    pilot-state.json
```

Each immutable sample retains the SHA-256 digest of its nested P6.1 result.
Every later invocation rebuilds qualification state from the sample files and
rechecks that referenced result digest.

Pre-pin local harness baseline (non-official):

```text
repositories: 3
passed samples: 4
failed samples: 0
total governed Extension Runs: 48
source repository mutations: 0
result integrity failures: 0
latency per Extension Run p50: 335.982 ms
latency per Extension Run p95: 514.074 ms
control data per 3-repository sample: about 119.5 KiB
observed duration: 0.001849 days
duration gate: ACTIVE
```

Source commits for that pre-pin local sample:

| Repository | Commit |
| --- | --- |
| `D:\AOS` | `0979e8ed265eab0842f9350e51fcf34597784bf2` |
| `D:\trenux_rust` | `3297389bd35ff3e8eb129dc74308ec3c8d165bf2` |
| `D:\Project\TRENUX` | `020b1ca41824bd0e13d7552136ec6fd1b8ba5f20` |

Harness markers:

```text
AOS_P6_2_QUALIFICATION_SAMPLE_OK
AOS_P6_2_DURATION_GATE_ACTIVE
```

CI uses `MinimumDurationDays 0` and one sample only to prove the harness works
on Windows and Ubuntu. That CI smoke does not replace the real installed-pilot
duration gate.

## Historical duration rule

The default installed pilot can move from `ACTIVE` to `QUALIFIED` only when:

```text
elapsed wall-clock duration >= 7 days
passed samples >= 7
failed samples = 0
sample result integrity failures = 0
every nested P6.1 pilot reports deterministic replay
every nested P6.1 pilot reports zero source mutation
```

Only then may the harness emit:

```text
AOS_P6_2_DURATION_GATE_OK
```

This historical lane stopped at two passing samples and `0.762998` real days.
It remains valid partial duration evidence but no longer blocks P6.2 after the
accepted ADR-0012 accelerated route passed.

## Accelerated qualification rule

The accelerated lane closes only when:

```text
passed samples >= 8
failed samples = 0
sample result integrity failures = 0
total governed Extension Runs >= 96
all nested capability outputs are deterministic
all nested results retain matching SHA-256
all source repositories remain unmodified
all pin hashes and exact repository commits remain unchanged
```

## Verification status

```text
P6.2 targeted process tests:       PASS (3)
P6.2 filesystem boundaries:        PASS locally
P6.2 governed reconciliation:      PASS locally
P6.2 installed-pilot harness:      PASS locally
P6.2 historical duration evidence: PARTIAL (2 samples, 0.762998 days)
P6.2 accelerated qualification:    PASS (8 samples, 96 Runs)
Rust unit/process tests:            PASS
Windows/Ubuntu hosted CI:          PASS
Production-like-runtime-ready:     BOUNDED PASS (declarative local slice)
Seven-day runtime resilience:      NOT CLAIMED
Production-ready:                  NOT CLAIMED
P7:                                PLANNED pending measured bottleneck
```

## Closeout and remaining boundaries

P6.2 has no remaining blocking work. A future duration soak becomes necessary
only if the runtime adds long-lived processes, network/provider effects,
distributed execution, or another measured risk that accelerated local samples
cannot represent. P7 still requires a measured scale, availability, or
coordination bottleneck; current P6.2 measurements do not establish one.

## Hosted provenance

Accelerated closeout:

```text
implementation commit: d7f7668258893f6ce4fa6af2fa68284614e7b641
GitHub Actions run: 30327707481 PASS
Windows job: 90176439798 PASS
Ubuntu job: 90176439734 PASS
P6.2 governance and regression gates: PASS on Windows and Ubuntu
P6.4/P6.5/P6.6 regression gates: PASS on Windows and Ubuntu
```

Original P6.2 implementation:

```text
implementation commit: 0979e8ed265eab0842f9350e51fcf34597784bf2
GitHub Actions run: 30253857069 PASS
Windows job: 89937806443 PASS
Ubuntu job: 89937806540 PASS
P6.2 filesystem fault-injection gate: PASS on Windows and Ubuntu
P6.2 installed-pilot harness smoke: PASS on Windows and Ubuntu
```
